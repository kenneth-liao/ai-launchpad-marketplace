---
description: Update the context system based on our conversation so far.
---

## Context System Check

Check that the context system has been initialized:

```bash
ls ~/.claude/.context/
```

If not, run `/personal-assistant:setup` first.

## Context Update Instructions

Read `~/.claude/.context/context-update.md` for the full update philosophy and instructions.

After completing the context update, you may finish your response.
